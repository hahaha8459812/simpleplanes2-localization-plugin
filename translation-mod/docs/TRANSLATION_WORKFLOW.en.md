# Translation Workflow

The standard translation workflow has been consolidated into the main README:

- [Back to main README: Standard Translation Workflow](../../README.en.md#standard-translation-workflow)

Short version: collect target UI text, use object paths for context, edit `zh-CN.fragments/*.json`, run `./build.ps1`, copy `zh-CN.json`, then press `F2` in game to verify.
